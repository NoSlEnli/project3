<html>
    <body>
        <H2>(<%= nItems %>) is found. There are (<%= nItems %>) devices. </H2>
        <ul>
            <% for (var i of items) { %>
            <li>Device ID: <a href="/details?_id=<%= i._id %>"><%= i.deviceID %></a></li>
            <% } %>
        </ul>
        <a href="/home">Back to home.</a>
    </body>
</html>
